import {PaginationItemsProvider} from '../../../src/pagination/PaginationItemsProvider';

describe('PaginationItemsProvider', () => {
  let paginationItemsProvider:PaginationItemsProvider;

  beforeEach(() => {
    paginationItemsProvider = new PaginationItemsProvider();
  });

  describe('What?', () => {
    it('Hello!', () => {
      // given

      // when

      // then
    });
  });
});
